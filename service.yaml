apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app.kubernetes.io/name: "backend"
  ports:
    - protocol: TCP
      port: 80
      targetPort: http # port name or 80
  type: ClusterIP